import { breakpoints, mediaQueries, radii, shadows, siteWidth, zIndices } from './base'
import { darkColors } from './color'
import { Theme } from './type'

const darkTheme: Theme = {
  breakpoints: breakpoints,
  colors: darkColors,
  mediaQueries: mediaQueries,
  radii: radii,
  shadows: shadows,
  siteWidth: siteWidth,
  zIndices: zIndices,
}

export default darkTheme
